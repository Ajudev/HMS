apiVersion: v1
kind: Service
metadata:
  name: hms-app-svc
  labels:
    app.kubernetes.io/name: hms-app-svc
spec:
  # if your cluster supports it, uncomment the following to automatically create
  # an external load-balanced IP for the frontend service.
  # type: NodePort
  ports:
  - port: 4567
    # nodePort: 30036
  selector:
    app.kubernetes.io/name: hms-app-svc